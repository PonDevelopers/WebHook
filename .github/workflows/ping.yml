name: Ping Bot
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Replit Bot
        run: curl https://donna-unprotruding-shin.ngrok-free.dev/webhook
